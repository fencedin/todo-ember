<h1>Done Tasks</h1>
  <ul>
    {{#each task in controller itemController='task'}}
      <li>{{input type="checkbox" class="toggle" checked=done}}
        {{#link-to 'task' task}}{{task.name}}{{/link-to}}
      </li>
    {{/each}}
  </ul>
